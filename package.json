{
  "name": "poe2-build-forge",
  "version": "0.0.0",
  "private": true,
  "description": "Convert Path of Exile 2 build guides into the in-game .build file format.",
  "license": "MIT",
  "packageManager": "pnpm@11.0.8",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --parallel build",
    "test": "pnpm -r test",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
